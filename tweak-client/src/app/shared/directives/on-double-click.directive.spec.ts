import { OnDoubleClickDirective } from './on-double-click.directive';

describe('OnDoubleClickDirective', () => {
  it('should create an instance', () => {
    const directive = new OnDoubleClickDirective();
    expect(directive).toBeTruthy();
  });
});
